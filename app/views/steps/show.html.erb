
<div id="notes-container">
  <%= form_with model: @step, remote: true, method: :patch, html: { id: "note-form" } do |f| %>
    <%= f.text_area :notes, placeholder: "Écrivez vos notes ici...", class: "form-control", rows: 30, data: { action: "input->step#autosave" } %>
  <% end %>
</div>

<div class= "d-flex justify-content-center">
  <%= link_to "Retour", goals_path, class: "btn btn-outline-dark mt-3" %>
</div>
